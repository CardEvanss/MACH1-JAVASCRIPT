// let primeiroNumero = 0;
// let segundoNumero = 0;
// let Soma = 0;

// primeiroNumero = 10;
// segundoNumero = 5;

// Soma = primeiroNumero + segundoNumero
// console.log('Soma', Soma)
// console.log(primeiroNumero + segundoNumero)
// console.log('tipo da variavel numero',typeof(numero));


{//PRIMEIRA RESPOSTA
    let pacotes = 7;
    let adesivos = 30;
    let repetidos = 15;
    let resultado = (pacotes*adesivos) - repetidos;
    console.log('Resultado:' ,resultado)
    }
    
    {//SEGUNDA RESPOSTA
    let caixa = 24;
    let quant = 4;
    let resultado = (caixa * quant);
    console.log('Latas:' ,resultado);
    }
    
    {//RESPOSTA 3
    let numUm = 10;
    numUm--;
    console.log(numUm);
    numUm--;
    numUm--;
    console.log(numUm);
    numUm--;
    numUm--;
    console.log(numUm);
    numUm--;
    numUm--;
    console.log(numUm);
    numUm--;
    numUm--;
    console.log(numUm);
    numUm--;
    console.log(numUm);
    }